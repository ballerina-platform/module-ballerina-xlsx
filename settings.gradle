/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 * For more detailed information on multi-project builds, please refer to https://docs.gradle.org/8.3/userguide/building_swift_projects.html in the Gradle documentation.
 */

pluginManagement {
    plugins {
        id "net.researchgate.release" version "${releasePluginVersion}"
        id "io.ballerina.plugin" version "${ballerinaGradlePluginVersion}"
        id "de.undercouch.download" version "${downloadPluginVersion}"
    }

    repositories {
        gradlePluginPortal()
        maven {
            url = 'https://maven.pkg.github.com/ballerina-platform/*'
            credentials {
                username System.getenv("packageUser")
                password System.getenv("packagePAT")
            }
        }
    }
}

plugins {
    id "com.gradle.develocity" version "3.19.2"
}

rootProject.name = 'module-ballerina-xlsx'

include ':xlsx-native'
include ':xlsx-ballerina'

project(':xlsx-native').projectDir = file("native")
project(':xlsx-ballerina').projectDir = file("ballerina")

include ':checkstyle'
project(':checkstyle').projectDir = file("build-config${File.separator}checkstyle")

develocity {
    buildScan {
        termsOfUseUrl = 'https://gradle.com/help/legal-terms-of-use'
        termsOfUseAgree = 'yes'
    }
}
